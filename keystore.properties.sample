# Copy this file to keystore.properties and fill in your actual values
# keystore.properties is gitignored and will not be committed

# Place your keystore file (e.g., release-key.jks) in the project root
# and reference it with a relative path like below:
storeFile=release-key.jks
storePassword=your-keystore-password
keyAlias=your-key-alias
keyPassword=your-key-password
